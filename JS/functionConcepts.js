import { LogMessage } from './utils.js';

// Functions
function Add1 (x, y) {
    return x + y;
}


// Arrow Functions
const Add2 = (x, y) => x + y;


// Anonymous Functions
(function (x, y) {
    return x + y;
});

(x, y) => {
    return x + y;
}

(x, y) => x + y;

x, y => x + y;
